<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Uprawnienia -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Opcjonalne funkcje -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDo">

        <!-- Deklaracje aktywności -->
        <activity android:name=".MainActivity" android:theme="@style/Theme.ToDo" android:exported="true"/>
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.ToDo" />
        <activity android:name=".AddTodosActivity" android:theme="@style/Theme.ToDo" />
        <activity android:name=".TodosActivity" android:theme="@style/Theme.ToDo" />
        <activity android:name=".DetailsActivity" android:theme="@style/Theme.ToDo" />

        <!-- Aktywność startowa -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>